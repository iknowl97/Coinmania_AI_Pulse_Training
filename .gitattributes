# Keep the shell script LF — CRLF would break the shebang / bash parsing on macOS
*.sh   text eol=lf

# Keep the PowerShell script CRLF for Windows (UTF-8 BOM is preserved as content)
*.ps1  text eol=crlf

# Web assets: LF
*.html text eol=lf
*.json text eol=lf
*.md   text eol=lf
